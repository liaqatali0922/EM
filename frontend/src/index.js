import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Importing the main component

ReactDOM.render(<App />, document.getElementById('root')); // Rendering the main component to the DOM
